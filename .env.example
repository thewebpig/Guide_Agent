OPENAI_MODEL=
OPENAI_API_KEY=
# API 根地址，例如 https://provider.example/v1；不要追加 /responses 或 /chat/completions。
OPENAI_BASE_URL=
# 可选择 responses（默认）或 chat_completions。
OPENAI_API_FORMAT=responses
# 本文件不会自动加载；请在终端设置环境变量或使用 start_demo.ps1 -ConfigureModel。
# GUIDE_SCENE_PATH=scenes/demo/scene.json
